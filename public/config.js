// Runtime config. In Docker this file is regenerated at container start
// from the SUPABASE_URL / SUPABASE_ANON_KEY environment variables.
// For local dev it stays empty and the app falls back to the .env file.
window.__APP_CONFIG__ = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
}
